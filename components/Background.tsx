/* Flat carbon page background with a static film-grain layer (see .bg-grain in
   globals.css). The old ambient glow + drift toggle are gone — the pink hero
   card is the page's one loud object; the background stays quiet. */
export const Background = () => <div aria-hidden className="bg-grain" />;
