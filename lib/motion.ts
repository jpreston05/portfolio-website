/* Framer twin of the CSS --ease-snappy token (globals.css) — the one easing
   curve used for UI motion across the site. */
export const EASE_SNAPPY: [number, number, number, number] = [0.23, 1, 0.32, 1];
